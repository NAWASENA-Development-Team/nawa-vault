# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# Bun lockfile — COMMIT ini ke repo (seperti package-lock.json)
# bun.lockb

# Lock files dari package manager lain — tidak dipakai
package-lock.json
yarn.lock
pnpm-lock.yaml
.pnp.*
.yarn/

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
bun-debug.log*

# env files — JANGAN commit file .env yang berisi secrets!
.env
.env.local
.env.production
.env.docker
# Contoh/template boleh di-commit
!.env*.example

# Vercel (tidak dipakai)
.vercel

# Netlify
.netlify/

# Docker
docker-compose.override.yml

# typescript
*.tsbuildinfo
next-env.d.ts
